# 🔐 إعداد مستخدم Admin - دليل الاستخدام

## المشكلة التي تم حلها
تم ربط صفحة إدارة المستخدمين بقاعدة البيانات، ولكن كان هناك خطأ "Unauthorized" عند إضافة مستخدم جديد.

### السبب
- صفحة تسجيل الدخول كانت تستخدم Mock Tokens (مثل `mock_admin_token`)
- الـ Backend يحتاج JWT Token حقيقي يحتوي على user_id
- تم تحديث الكود ليستخدم `"auth_token"` في جميع الأماكن

## ✅ الحل

### الخطوة 1: إنشاء مستخدم Admin للاختبار

قم بتشغيل السكريبت التالي لإنشاء مستخدم admin:

```bash
cd api
python create_test_admin.py
```

**بيانات المستخدم:**
- 📧 البريد الإلكتروني: `admin@audit.com`
- 🔑 كلمة المرور: `admin123`
- 👤 الاسم: مدير النظام
- 🎭 الدور: Admin

### الخطوة 2: تشغيل الـ Backend

```bash
cd api
uvicorn app.presentation.main:app --reload
```

### الخطوة 3: تشغيل الـ Frontend

```bash
cd frontend
npm run dev
```

### الخطوة 4: تسجيل الدخول

1. افتح المتصفح على `http://localhost:3000/login`
2. أدخل البيانات:
   - البريد الإلكتروني: `admin@audit.com`
   - كلمة المرور: `admin123`
3. انقر "تسجيل الدخول"

### الخطوة 5: إضافة مستخدم جديد

1. انتقل إلى صفحة الأدمن: `/admin`
2. انقر على "إدارة المستخدمين" في القائمة الجانبية
3. انقر على زر "مستخدم جديد"
4. املأ النموذج:
   - الاسم الكامل (مطلوب)
   - البريد الإلكتروني (مطلوب)
   - كلمة المرور (8 أحرف على الأقل)
   - الدور (اختياري، القيمة الافتراضية: User)
5. انقر "إضافة"

## 📊 التغييرات التي تمت

### 1. `types/admin.ts`
- ✅ تحديث واجهة `AdminUser` لتتوافق مع API
- ✅ الحقول الجديدة: `created_at`, `updated_at`, `locale`, `timezone`, `active`

### 2. `hooks/use-admin.ts`
- ✅ إضافة `useCreateUser()` mutation
- ✅ تحديث `getAuthHeaders()` لاستخدام `"auth_token"`
- ✅ إعادة تحميل قائمة المستخدمين بعد الإضافة الناجحة

### 3. `app/admin/page.tsx`
- ✅ إزالة جميع البيانات الوهمية للمستخدمين
- ✅ عرض البيانات الحقيقية من API فقط
- ✅ تحديث جدول المستخدمين (عمود تاريخ الإنشاء بدلاً من آخر تسجيل دخول)
- ✅ إضافة نموذج إضافة مستخدم مع جميع الحقول المطلوبة
- ✅ معالجة حالة القائمة الفارغة
- ✅ عرض حالة loading أثناء الإضافة

## 🔍 استكشاف الأخطاء

### خطأ "Unauthorized"
- ✅ **تم الحل:** تأكد من تسجيل الدخول باستخدام حساب حقيقي وليس Mock Token
- ✅ **تم الحل:** تحديث `getAuthHeaders()` لاستخدام `"auth_token"` بدلاً من `"token"`

### خطأ "Failed to fetch"
- تأكد من تشغيل Backend API على `http://localhost:8000`
- تحقق من إعدادات CORS في Backend

### خطأ "Validation Error"
- تأكد من ملء جميع الحقول المطلوبة (الاسم، البريد، كلمة المرور)
- كلمة المرور يجب أن تكون 8 أحرف على الأقل

## 🎯 الميزات المتاحة حالياً

- ✅ عرض قائمة المستخدمين من قاعدة البيانات
- ✅ إضافة مستخدم جديد
- ✅ عرض تفاصيل المستخدم (الاسم، البريد، الدور، الحالة، تاريخ الإنشاء)
- ✅ البحث والفلترة (من خلال واجهة المستخدم)

## 📝 ميزات مستقبلية (يمكن إضافتها)

- 🔄 تعديل بيانات المستخدم
- 🗑️ حذف المستخدم
- 🔐 تغيير كلمة المرور
- ✅ تفعيل/تعطيل المستخدم
- 📊 عرض نشاط المستخدم
- 🎭 تعديل الأدوار والصلاحيات

## 📞 الدعم

إذا واجهت أي مشكلة، تحقق من:
1. ✅ Backend API قيد التشغيل
2. ✅ قاعدة البيانات متصلة
3. ✅ تسجيل الدخول بحساب حقيقي
4. ✅ الـ token صالح ولم ينتهِ

---

**تم الإنجاز بتاريخ:** 30 أكتوبر 2025  
**الحالة:** ✅ جاهز للاستخدام
